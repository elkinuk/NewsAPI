import { FilterByMinePipe } from './filter-by-mine.pipe';

describe('FilterByMinePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByMinePipe();
    expect(pipe).toBeTruthy();
  });
});
